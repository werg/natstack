/**
 * @natstack/panel
 *
 * Core panel API for NatStack panels. This provides:
 * - Panel bridge API (createChild, setTitle, close, etc.)
 * - RPC communication with parent/child panels and workers
 * - Theme integration
 * - Git configuration access
 *
 * For React helpers, also add @natstack/react.
 * For AI utilities, also add @natstack/ai.
 */

// Re-export everything from @natstack/core
export * from "@natstack/core";
