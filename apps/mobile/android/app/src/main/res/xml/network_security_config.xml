<?xml version="1.0" encoding="utf-8"?>
<!--
  Release-safe network security config.

  Default posture: HTTPS-only (cleartextTrafficPermitted="false").

  Exception list for dev-friendly hosts that remain reachable over HTTP in
  release builds so Tailscale/MagicDNS users can still connect without
  provisioning a TLS cert. Exceptions are scoped narrowly:

    - localhost / 127.0.0.1   — in-device loopback only
    - 10.0.2.2                — Android emulator host-loopback alias
    - ts.net (w/ subdomains)  — Tailscale MagicDNS hostnames; traffic over
                                a Tailscale tailnet is already end-to-end
                                encrypted at the tailnet layer.

  Debug builds override this file from src/debug/res/xml/ with a permissive
  config for iteration against arbitrary LAN IPs.

  Reference: https://developer.android.com/training/articles/security-config
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="true">ts.net</domain>
    </domain-config>
</network-security-config>
