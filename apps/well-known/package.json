{
  "name": "@natstack/well-known",
  "private": true,
  "scripts": {
    "build:site": "tsx build.ts",
    "dev": "wrangler pages dev dist",
    "deploy": "wrangler pages deploy dist"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "wrangler": "^4.0.0"
  }
}
