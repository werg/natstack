# Fixer Review — verification of the 44-finding fix pass (2026-06-16)

Independent per-finding verification of the developer agent's fixes against the current working tree. **36/44 cleanly fixed**; 6 fixed-with-concerns, 1 partial, 1 not-addressed, **4 regressions**.

Also: `tsc --noEmit` clean; full vitest suite green (3427 passed) AFTER fixing 2 stale builder.app tests (entry bundles are now content-hashed) and the configLoader bundle-resolution regression below.

**Resolved during this review (by the reviewer):**
- 2 stale `builder.app.test.ts` assertions updated to expect content-hashed entry bundles.
- **#31 loader half — FIXED:** `configLoader.ts` now captures `document.currentScript.dataset.bundleSrc` synchronously at the top of the IIFE (it was read after two `await`s → null → fell back to the no-longer-existing `./bundle.js` → 404 / blank panel). The serving-side + security half of #31 was already correctly fixed by the developer.

**ALL OPEN PROBLEMS NOW RESOLVED (by the reviewer, 2026-06-16) — each adversarially re-verified (probe tests), `tsc` clean, full suite green:**
- **#1** (file↔dir data-loss): `materializeState` deletes stale paths BEFORE writes + `clearNonDirAncestors`/`rmTolerant` (`store.ts`). Tests: file→dir, dir→file, untracked-conflict.
- **#4** (GitBridge deletion-loss): sidecar kept OUTSIDE gitDir via new `MaterializeOptions.sidecarDir` (`gitBridge.ts`, `store.ts`); `.gad` no longer wiped. Test: cross-transition deletion propagates, no `.gad` leak.
- **#11** (non-content conflict visibility): `MERGE_CONFLICTS.md` worktree summary driven off the pending-merge record, **ignored only at worktree root** (no basename shadowing), removed on resolve/abort (`workspaceVcs.ts`, `store.ts`). Tests: summary write, resolve-removes, abort-removes, root-only ignore. (PublishBar already renders `conflict.kind`.)
- **#2** (merge exec-bit drop): independent 3-way `resolveMode` in both the same-content and diff3 arms (`merge.ts`). Test: content-edit + mode-flip preserves both.
- **#14** (effectFailedStep): explicit `publish_envelope` drop (it was wrongly closing the turn) + compile-time `EffectKind` exhaustiveness guard (`step.ts`). Verified: adding a 7th kind fails `tsc`.
- **#38** (PanelHandle.on): `refreshMetadata` non-null fallback `rpcTargetId ?? id` (`handles.ts`).

Adjacent observations (pre-existing, NOT introduced here): #38's broader async-resolution refactor drops events that arrive during the one-time lazy `resolveRpcTargetId()` window for unloaded refreshable handles; #2's binary/delete-vs-change arms can drop the other side's mode change but always flag a conflict (manual-resolution case, out of the silent-drop scope).

(Original open list, now closed: #1, #4 P1 regressions; #11 P2 not-addressed; residuals #2/#14/#38.)


---

## REGRESSIONS (fixer introduced a new problem)

### #1 [P1] materializeState crashes on any file↔directory path-type transition (checkout primitive throws)
- status: **partial** | fixCorrect: false | regressionIntroduced: **true**
- reasoning: The developer changed real code (added a staleAncestors pre-deletion loop at 452-458 and switched the two fsp.rm calls at 482 and 500 to recursive:true), so the cited code is not "unchanged" — but the bug is not resolved. I verified by writing transition tests and running them against the current tree. file→dir no longer throws yet produces an EMPTY directory: the post-write stale-deletion loop (still ordered after the write loop) does a recursive rm of the old file path `foo`, which is now the directory containing the freshly-written `foo/bar.ts`, destroying it — a new data-loss regression that is arguably worse than the original throw because it silently yields a corrupt checkout. dir→file still throws, now ENOTDIR at the deletion loop instead of EISDIR at copy. And the staleAncestors guard only covers conflicting paths recorded in the sidecar, so a conflicting untracked/external on-disk file still makes mkdir throw EEXIST. Because the happy path the finding called out (file↔dir across two materializeState calls — the checkout primitive used by context folders, merge, fork, git bridge) is still broken, and the fix introduced silent data loss, this is partial with a regression. The suggestedFix's requested tests were also not added.
- residual/fix: Make the stale/conflict deletion robust against path-type transitions and order it so it cannot delete newly-written content. Concretely: (1) Run the full stale-path deletion (the loop over old sidecar keys not in target) BEFORE the write loop, not after — currently it runs after the write loop and recursively deletes the directory the write loop just created (file→dir loses data). (2) For each target path, before mkdir/copy, walk every ancestor path component of absPath and remove any component that exists on disk as a non-directory (covers file→dir even when the conflicting on-disk path is not in the sidecar — the developer's staleAncestors check only inspects Object.keys(sidecar.files), so an externally-created or untracked conflicting file still makes mkdir throw EEXIST/ENOTDIR). (3) Guard the deletion loop's rm of stale child paths so it doesn't traverse through a path whose ancestor is now a file (dir→file currently throws ENOTDIR at lstat of dir/foo/bar.ts because foo is now a regular file). Add tests for file→dir and dir→file across two successive materializeState calls into the same dir (as the finding's suggestedFix requested) — these are still absent from store.test.ts.
- evidence: CURRENT src/server/gadVcs/store.ts materializeState. Pre-delete of stale ancestors (lines 452-458):
  const staleAncestors = Object.keys(sidecar.files).filter(
    (relPath) => !targetPaths.has(relPath) && target.some((file) => file.path.startsWith(`${relPath}/`)));
  for (const relPath of staleAncestors) { await fsp.rm(path.join(dir, ...relPath.split("/")), { force: true, recursive: true }); }
Write loop now uses recursive rm before copy (line 482): `await fsp.rm(absPath, { force: true, recursive: true });`
But the STALE-DELETION loop still runs AFTER the write loop (lines 498-503):
  for (const relPath of Object.keys(sidecar.files)) {
    if (!targetPaths.has(relPath)) { await fsp.rm(path.join(dir, ...relPath.split("/")), { force: true, recursive: true }); deleted += 1; } }
Empirical proof (probe tests I ran against the current code, then deleted):
- file→dir (old file `foo` → target `foo/bar.ts`): materializeState returns {written:1, deleted:1} (no throw) but final readTree(out) === {} — the post-write deletion loop sees `foo` in old sidecar, not in targetPaths, and does rm(dir/foo,{recursive:true}), recursively deleting the just-written `foo/bar.ts`. NEW data-loss regression.
- dir→file (old `foo/bar.ts`,`foo/baz.ts` → target file `foo`): throws `ENOTDIR: not a directory, lstat '.../o2/foo/bar.ts'` from the deletion loop rm(dir/foo/bar.ts) because `foo` is now a regular file. Still throws.
- file→dir where conflicting on-disk `foo` is NOT in the sidecar (external/untracked): throws `EEXIST: file already exists, mkdir '.../foo'` at store.ts:478, because staleAncestors only inspects Object.keys(sidecar.files).

### #4 [P1] GitBridge.exportHead commits the internal `.gad/CHECKOUT.json` sidecar into every exported git commit
- status: **fixed-with-concerns** | fixCorrect: true | regressionIntroduced: **true**
- reasoning: I added a focused vitest harness alongside gitBridge.test.ts: two transitions where the second deletes b.txt, then exportHead, then `git ls-tree -r --name-only HEAD`. Result: the `.gad` assertion PASSED (the original P1 sidecar-pollution bug is genuinely fixed — `.gad/CHECKOUT.json` is no longer in any exported commit), but `b.txt` remained in the HEAD tree even though it was deleted in the second VCS transition — proving the blanket `.gad` removal breaks materializeState's sidecar-driven deletion detection. The existing gitBridge.test.ts only ever adds files (a.txt/b.txt/c.txt), never deletes one across transitions, so it green-lights this regression. The file is untracked at HEAD (new in the unified-log rewrite), which is why `git diff HEAD` is empty; line 132 is the developer's added fix and is present in the working tree. Net: described bug resolved, but a new P1-class data-fidelity regression (lost deletions in the GitHub interchange export) was introduced.
- residual/fix: Stop wiping the sidecar between export iterations. materializeState's deletion loop (store.ts:498, `for (const relPath of Object.keys(sidecar.files))`) relies on the prior-iteration sidecar to know which previously-materialized files to delete from the worktree. Removing the whole `.gad` dir after every materializeState gives the next iteration an empty sidecar (readSidecar "cache amnesia"), so cross-transition file deletions never get applied to gitDir and stay committed forever. Fix by excluding `.gad` at the git layer instead of deleting it — e.g. write `.git/info/exclude` with a `.gad/` line once after init (addAll skips git-ignored files, client.ts), or remove `.gad` only on the final iteration / restore it after addAll, or add a noSidecar/sidecar-path option to materializeState for bridge use so the cache lives outside gitDir.
- evidence: src/server/gadVcs/gitBridge.ts:131-133 (current): `await vcs.vcs.materializeState(entry.outputStateHash, gitDir);` then NEW line 132 `await fsp.rm(path.join(gitDir, ".gad"), { recursive: true, force: true });` then `await this.git.addAll(gitDir);`. The `.gad` removal is correctly sequenced before addAll, so the sidecar no longer reaches the commit. But store.ts:516 `await this.writeSidecar(dir, { version: 1, stateHash, files: entries });` writes the sidecar each call, and store.ts:498 `for (const relPath of Object.keys(sidecar.files)) { if (!targetPaths.has(relPath)) { await fsp.rm(...) ... } }` is the ONLY deletion path — it reads the sidecar produced by the PRIOR materializeState (store.ts:447 `const sidecar = await this.readSidecar(dir)`), which line 132 just destroyed. readSidecar (store.ts:243-246) swallows the missing file and returns `{ files: {} }`, so the deletion loop iterates over nothing.

### #31 [P2] Ref-pinned panel can be served the wrong entry bundle via a referer-less sub-resource request
- status: **fixed-with-concerns** | fixCorrect: true | regressionIntroduced: **true**
- reasoning: The developer correctly applied the suggestedFix's primary recommendation (content-hash the entry bundle, point HTML at the hashed entry), which genuinely closes the ref-pinning hole described in finding #31: build identity is now carried by the bundle URL, so a referer-less sub-resource request for a hashed bundle that is absent from the resolved build 404s (via the new isPanelAssetRequest guard) rather than silently cross-serving a different state's entry code. The referer parsers were kept but are now harmless. However, the wiring of the hashed bundle name into the runtime loader is broken: configLoader.ts reads document.currentScript after at least one await (transport.js load is unconditional), where currentScript is null per HTML semantics. Apps (usePanelLoader:false) are unaffected because their script src is rewritten to the hashed name directly in injectHtmlTransforms (builder.ts:1313-1317), but every loader-based panel (all non-app panels, including chat) would request ./bundle.js, get a 404, and fail to boot its entry bundle. This is a new, broad functional regression introduced by the fix. I rate fixCorrect=true because the change does resolve the described security bug, but the regression downgrades the overall verdict to fixed-with-concerns and is verified by reading configLoader.ts and the build artifact path logic; the entryWrapper test only checks the emitted HTML data-bundle-src, not the runtime currentScript read, so it does not catch this.
- residual/fix: Fix the runtime loader's bundle-src resolution: `document.currentScript` is read after `await`s in CONFIG_LOADER_JS, so it is always null at that point and the bundle falls back to the no-longer-existing `./bundle.js` (404). Capture `document.currentScript.dataset.bundleSrc` synchronously at the very top of the IIFE (before any await) and use the captured value. Optionally remove the now-redundant refFromReferer/contextIdFromReferer heuristics since build identity is carried by the URL.
- evidence: The DESCRIBED bug is fixed via the recommended altitude fix — content-hashed entry bundles. src/server/buildV2/builder.ts:1932 `entryNames: \"[name]-[hash]\"` (was `\"[name]\"` per `git diff HEAD`); builder.ts:1981 stores `path: bundleArtifactPath` (=`bundle-{hash}.js`); builder.ts:2034 `bundleSrc: relativeAssetHref(bundleArtifactPath)`. So a referer-less request for `bundle-{hash}.js` against the wrong (main-HEAD) build no longer matches an artifact and, per src/server/panelHttpServer.ts:744-748 `if (isPanelAssetRequest(resource)) { res.writeHead(404 ... }`, returns 404 instead of cross-serving main's bundle — ref-pinning integrity is restored. BUT the loader regression: src/server/configLoader.ts:134-141 `const currentScript = document.currentScript; const configuredBundleSrc = currentScript && currentScript instanceof HTMLScriptElement ? currentScript.dataset.bundleSrc : null; ... bundle.src = configuredBundleSrc || \"./bundle.js\";`. This read at line 134 runs only after `await new Promise(...)` (transport.js load, line 124, unconditional) and `await shell.getPanelInit()` (line 70), so `document.currentScript` is null (spec: reset after synchronous script execution yields). configuredBundleSrc is therefore always null → `bundle.src=\"./bundle.js\"`, which no longer exists as an artifact → 404 → entry code never loads.

### #38 [P3] PanelHandle.on() event delivery becomes async + storms metadata RPCs when rpcTargetId is null
- status: **fixed-with-concerns** | fixCorrect: true | regressionIntroduced: **true**
- reasoning: Both reported defects are resolved. (1) Async reordering: the per-event path is now fully synchronous — it reads the cached `rpcEventTargetId` variable inside the rpc.on callback with no .then(), so two back-to-back events cannot reorder. (2) RPC storm: resolution is performed at most once per subscription, memoized through `rpcTargetResolvePromise`, and the per-event callback never calls resolveRpcTargetId()/refreshMetadata(), so there is no O(events) metadata round-trip amplification for unloaded panels. The negative resolution is effectively cached because the .then sets rpcEventTargetId to fresh.id (the fallback) rather than leaving it null. So fixCorrect=true for the described bug. However the fix introduces a new, narrower bug (regressionIntroduced=true): refreshMetadata() uses a different fallback (null for refreshable handles) than resolveRpcTargetId()'s .then (fresh.id). Because refresh()/ensureLoaded() invoke refreshMetadata() directly, calling them on a still-unloaded refreshable handle resets rpcEventTargetId to null, silently disabling any active .on() subscription with nothing to re-resolve it. There is also a minor startup drop-window where events arriving during the one-time async resolution are dropped (the pristine original had no such window since metadata.id was synchronously available). These are subtler than the original storm/reorder and only affect unloaded refreshable handles, so the verdict is fixed-with-concerns rather than partial.
- residual/fix: Make refreshMetadata() use the same non-null fallback as resolveRpcTargetId() (i.e. set rpcEventTargetId = metadata.rpcTargetId ?? metadata.id) so a manual handle.refresh()/ensureLoaded() on a still-unloaded refreshable handle does not reset rpcEventTargetId back to null and silently kill an active .on() subscription. Optionally re-trigger resolution after refreshMetadata when there are live listeners, and consider buffering events that arrive during the one-time subscription-resolution window for unloaded panels.
- evidence: handles.ts:161-169 on() now reads a cached id synchronously per event (no per-event RPC, no async reorder): `on(event, listener){ if(!rpcEventTargetId){ void resolveRpcTargetId().catch(()=>undefined); } return rpc.on(event,(ev)=>{ const targetId=rpcEventTargetId; if(targetId && ev.caller.callerId===targetId) listener(ev.payload); }); }`. handles.ts:96-105 resolveRpcTargetId() memoizes via `rpcTargetResolvePromise ??= refreshMetadata().then(fresh=>{ const targetId=fresh.rpcTargetId ?? fresh.id; rpcEventTargetId=targetId; return targetId; })` — one refresh per subscription, falls back to fresh.id (never null). REGRESSION at handles.ts:92: refreshMetadata sets `rpcEventTargetId = metadata.rpcTargetId ?? (!ops?.refresh ? metadata.id : null)` — for a refreshable still-unloaded handle this is null, so a manual refresh()/ensureLoaded() (handles.ts:176-187) resets rpcEventTargetId to null and an existing subscription stops matching with nothing re-resolving it. createRuntime.ts:97,109 confirm hydrated handles carry rpcTargetId:null with ops.refresh set — exactly the affected case.

---

## NOT-ADDRESSED

### #11 [P2] diff3-clean and mode/binary/delete-vs-change conflicts share no worktree markers, so non-content conflicts are invisible in the tree
- status: **not-addressed** | fixCorrect: false | regressionIntroduced: **false**
- reasoning: The entire src/server/gadVcs/ directory is untracked (new in this rewrite), so `git diff HEAD` shows nothing — but reading the CURRENT code shows the non-content conflict handling is exactly as the finding described. mode/binary/delete-vs-change conflicts still materialize a clean-looking file with no `<<<<<<<` markers, no sidecar conflict summary (the only sidecar, .gad/CHECKOUT.json, is a checkout-state cache per store.ts), and no commit-blocking acknowledgement. The conflicts[] array still carries the kind, but the finding explicitly already deemed that insufficient because nothing in the worktree warns the user. The suggested fix (sidecar/summary file, or block resolving commit, or render kind prominently in caller) was not implemented anywhere I could find via grep across src/server. No regression was introduced because nothing in this area changed relative to the described behavior; the bug simply persists.
- residual/fix: Surface non-content conflicts in the worktree: write a sidecar/conflict-summary file (or block the resolving commit until each non-content conflict path is acknowledged) for `mode`, `binary`, and `delete-vs-change` conflicts before materializeState, and/or have the vcsService caller render conflict.kind prominently. Add tests covering worktree visibility for these three kinds (current merge.test.ts only asserts `content` markers).
- evidence: src/server/gadVcs/merge.ts still keeps clean content verbatim with no marker for the three non-content kinds: line 171-172 `keep(o.mode === t.mode ? o : { ...o, mode: 33188 }); if (o.mode !== t.mode) conflicts.push({ path, kind: "mode" });`; line 178-179 `conflicts.push({ path, kind: "delete-vs-change" }); keep((o ?? t)!);`; line 189-190 `conflicts.push({ path, kind: "binary" }); keep(o); // ours wins provisionally`. Only the `content` branch embeds markers via diff3Merge (line 193-207). In src/server/gadVcs/workspaceVcs.ts:928-951 the conflicted path stages `result.files` and `materializeState(staged.stateHash, targetDir)` directly with no conflict-summary/sidecar file and no commit gate — the `conflicts` array is parked only in pendingInfo. src/server/services/vcsService.ts:301-313 merely returns `result` (incl. conflicts) to the caller; nothing surfaces kind in the tree. merge.test.ts asserts markers only for the `content` case (line 154-160), no test added for mode/binary/delete-vs-change worktree visibility.

---

## FIXED-WITH-CONCERNS (resolved, residual noted)

### #2 [P1] Mode-only change on one side is silently dropped during merge (executable bit lost)
- status: **fixed-with-concerns** | fixCorrect: true | regressionIntroduced: **false**
- reasoning: git diff HEAD showed nothing for merge.ts because the file is untracked (new file in the rewrite); I read the current source directly. The described bug — base mode 0644, theirs flips executable with identical content, ours untouched — is now correctly handled: mode is folded into theirsChanged (line 154), so theirsChanged=true / oursChanged=false routes to keep(t) which carries theirs' 0755 mode, and materialization (store.ts:484) chmods it executable. No conflict is needed because only one side changed, matching standard 3-way merge semantics. The fix also added a mode conflict in the both-changed-same-content arm (line 172), which is a correct enhancement, not a regression. No invariant is broken: keep() always carries file.mode, modes are binary (33188/33261) so a mode-only divergence with identical content can never produce a divergent result. I mark fixed-with-concerns rather than fixed because a related arm (one side content-change, other side mode-change) still silently forces mode to 33188 at line 205 with no mode conflict, dropping a legitimate executable bit — a narrower instance of the same class of loss the finding warned about, left unaddressed.
- residual/fix: Edge case still drops the mode silently: when ONE side changes content and the OTHER side changes only the mode (e.g. ours edits content keeping 0644, theirs flips to 0755 with base content), both sides are "changed" and content_hash differs, so control reaches the diff3 arm where line 205 sets `mode: o.mode === t.mode ? o.mode : 33188`. This forces 0644 and discards theirs' executable bit with NO mode conflict flagged. The finding's primary case (mode-only divergence on one side) is fully fixed; this content-vs-mode interaction is a narrower residual that could be closed by flagging a mode conflict (and/or preferring the changed mode) in the diff3 arm.
- evidence: src/server/gadVcs/merge.ts:150-155 now folds mode into change detection:
```
const oursChanged =
  (o?.content_hash ?? null) !== (b?.content_hash ?? null) ||
  (o?.mode ?? null) !== (b?.mode ?? null);
const theirsChanged =
  (t?.content_hash ?? null) !== (b?.content_hash ?? null) ||
  (t?.mode ?? null) !== (b?.mode ?? null);
```
Bug scenario (base 0644, theirs 0755 same content, ours untouched): oursChanged=false (content & mode equal base), theirsChanged=true (mode differs). Reaches line 165-167 `if (theirsChanged && !oursChanged) { if (t) keep(t); }`, and keep() (line 142-144) carries `mode: file.mode`, so theirs' 0755 is preserved; materialization at store.ts:484-485 then chmods 0755. Bug resolved.
Both-changed-same-content arm now also flags mode conflict: merge.ts:170-172 `if (o && t && o.content_hash === t.content_hash) { keep(o.mode === t.mode ? o : { ...o, mode: 33188 }); if (o.mode !== t.mode) conflicts.push({ path, kind: "mode" }); }`.
Residual gap at merge.ts:205 `mode: o.mode === t.mode ? o.mode : 33188` in the diff3 arm drops a diverging executable bit silently.

### #14 [P2] effectFailedStep has no fail-closed exhaustiveness guard; an unmapped effect-id prefix returns EMPTY and the driver re-dispatches/fails forever (AL-7 fix incomplete)
- status: **fixed-with-concerns** | fixCorrect: true | regressionIntroduced: **false**
- reasoning: The developer replaced the old "treat anything as an invocation and return EMPTY when the pending is absent" catch-all with an explicit branch (step.ts:877) that, for any effect-id not matching model (by kind), credential (by kind), form:, or inv:, appends a published `unmapped_effect_failure` diagnostic plus a `turn.closed` terminal instead of silently returning EMPTY. This correctly resolves the DESCRIBED bug's practical symptom for the today/open-turn path: a failure of an unmapped effect now produces a visible fail-closed terminal rather than the silent EMPTY that wedged the driver, and the concrete AL-7 form-delivery case is fully fixed and covered by a test. So the bug as experienced is no longer present and fixCorrect is true. However, the finding explicitly demanded a STRUCTURAL exhaustiveness guard ("an explicit dispatch over the known prefixes and add a default: const _exhaustive: never ... so an unmapped effect prefix is impossible to ship silently"). That guard is still not present: dispatch remains string-prefix-based with no compile-time `never` check, so adding a new EffectKind to the union compiles cleanly with zero warning. Worse, the runtime fail-closed path only emits a diagnostic + turn.closed; it does not clear a hypothetical new pending state in fold, so a genuinely new derivable effect kind would remain in derivePendingEffects after the turn closes and could still be re-dispatched/re-failed (the diagnostic is deduped by stable envelopeId so the log won't grow, but the latent re-dispatch wedge is not structurally eliminated). Additionally, when no turn is open the unmapped branch returns EMPTY exactly as before. No regression is introduced — the change is strictly safer than the prior EMPTY-only behavior. Verdict: fixed-with-concerns — the happy path and the originally-reported concrete wedge are resolved, but the exhaustiveness guarantee the finding called for is only partially realized.
- residual/fix: Replace the prefix-string dispatch with a true exhaustiveness guard: switch over EffectKind (or the prefix set model:/inv:/form:/credwait:) with a `default: { const _exhaustive: never = kind; throw }` so a newly added EffectKind fails at compile time. Also (a) emit a fail-closed terminal even when no turn is open (currently returns EMPTY when `turn` is falsy), and (b) ensure the fail-closed path for an unknown prefix actually clears the corresponding pending state in fold so the effect stops being re-derived — `turn.closed` alone does not clear a hypothetical new pending map, so the re-dispatch wedge could persist (the diagnostic is deduped by stable envelopeId, but the effect stays derivable).
- evidence: step.ts:877-902 (new catch-all for unmapped prefixes): `if (!incoming.effectId.startsWith("inv:")) { const append: AppendItem[] = []; if (turn) { const messageId = \`diag:${turn.turnId}:unknown-effect:${incoming.effectId}\`; append.push({ ... metadata: { code: "unmapped_effect_failure", severity: "error" } ...}); append.push(turnClosedItem(turn.turnId, { reason: "work_failed" })); } return { append, effects: [] }; }`. The four derivable prefixes are exactly model:/inv:/form:/credwait: (ids.ts:99-113; derivePendingEffects effects.ts:210-223). model_call/credential_wait dispatch by `incoming.kind` (step.ts:783, 827), form: and inv: by effectId prefix (step.ts:850, 905). There is NO `switch`/`never` over EffectKind. When `turn` is falsy the catch-all returns `{ append: [], effects: [] }` == EMPTY (step.ts:34 `const EMPTY: StepOutput = { append: [], effects: [] }`). The concrete form-delivery wedge is now fixed+tested (agent-loop.test.ts:459-476: form: effect resolved fail-closed, pendingApprovals cleared).

### #24 [P2] User approval for a main-head advance is awaited while holding the main-head serialization lock, stalling all other main operations
- status: **fixed-with-concerns** | fixCorrect: true | regressionIntroduced: **false**
- reasoning: The developer applied exactly the suggested fix to the two paths cited as the bug's locations (commitHead and mergeHeads): compute the candidate under the lock, release the lock, run the approval (beforeAdvance), then re-acquire the lock to ingest while re-validating that the head has not moved (SnapshotApprovalStaleError → retry loop). This eliminates the lock-held-across-human-wait for every reachable main-advance approval: userland callers (the only ones that produce a blocking human prompt) reach main only through publish, which goes through the now-unlocked mergeHeads path; the freshness-scan commitHead(VCS_MAIN_HEAD) calls pass no beforeAdvance and so only queue behind the brief prepare/finalize windows. A dedicated regression test proves the lock is released during the wait and that concurrent main writes proceed, with correct stale-retry merge semantics. No new bug is introduced: the re-validation correctly aborts and retries if state moved during the wait, and the test confirms the folded result. I mark fixed-with-concerns rather than fixed only because the structurally identical pattern remains in applyEdits/revert (beforeAdvance still invoked inside locked()). That is currently unreachable as a human-blocking main wait given the write-head gating and privileged-caller short-circuit, so the described availability bug is resolved in practice, but the latent shape is worth flagging in case future wiring lets a userland caller advance main through applyEdits/revert.
- residual/fix: Optionally restructure applyEdits/revert with the same prepare/approve-unlocked/finalize split for symmetry and future-proofing, since they still call beforeAdvance inside this.locked(input.head, ...) (workspaceVcs.ts:1364, 1426). Today this is not reachable as a human-blocking main wait (userland callers can't target head=main via applyEdits/revert; privileged callers short-circuit the gate), so it is latent rather than active. Also consider that mergeHeadsWithUnlockedApproval issues a pre-merge commitHead(targetHead, {beforeAdvance}) (line 968) which could in principle surface a second approval prompt if main is dirty at publish time — benign because the gate early-returns when changedPaths is empty, but worth a note.
- evidence: FIX (commit path) workspaceVcs.ts:281-285 routes to unlocked variant; 352-373 commitHeadWithUnlockedApproval: `const prepared = await this.prepareCommitSnapshot(...)` (under lock) → `await opts.beforeAdvance(prepared.candidate.event)` (NO lock held) → `finalizeApprovedCommitSnapshot(...)` (re-acquires lock); on SnapshotApprovalStaleError it `continue`s. prepareCommitSnapshot wraps work in `this.locked(head, ...)` (388) and throws out via SnapshotPreparedError to release before the wait. finalizeApprovedCommitSnapshot re-validates: `if (candidate.previousStateHash !== approved.previousStateHash || candidate.stateHash !== approved.stateHash) throw new SnapshotApprovalStaleError();` (453-456) plus expectedRefStateHash guard (460).
FIX (merge/publish path) workspaceVcs.ts:834-838 routes mergeHeads to mergeHeadsWithUnlockedApproval; 966-986: prepareMergeAdvance under lock → `await opts.beforeAdvance(prepared.event)` unlocked → finalizeApprovedMerge re-acquires lock and re-validates `if (currentTarget !== prepared.oursState || currentSource !== prepared.theirsState) throw new SnapshotApprovalStaleError();` (1161-1162). vcsService.ts publish (340-371) uses vcs.mergeHeads(VCS_MAIN_HEAD,...) — the only userland route to main.
REACHABILITY of the residual gap: applyEdits still holds the lock across approval — workspaceVcs.ts:1266 `return this.locked(input.head, async () => {` wraps the whole body including `await input.beforeAdvance(event)` at 1364 and 1426; revert delegates to applyEdits (1656-1662). But resolveWriteHead (vcsService.ts:69-97) confines non-privileged callers to their ctx head, and the gate short-circuits privileged callers: mainAdvanceApproval.ts:100 `if (runtimeKind === "shell" || runtimeKind === "server" || runtimeKind === "harness") return;`. Only userland (panel/app/worker/do) trigger the blocking approvalQueue.request (mainAdvanceApproval.ts:142, 178) and they reach main solely via publish→mergeHeads (fixed).
BOOT-STALL resolved: bootstrap scan commitHead(VCS_MAIN_HEAD) at workspaceVcs.ts:239 passes NO beforeAdvance → locked fast path, no human wait.
TEST: workspaceVcs.mainApproval.test.ts:61 "does not hold the main-head lock while approval is pending" passes (a second commit completes within 250ms while the first commit's approval is pending; stale retry folds in second.txt). Verified: vitest run → 1 passed.
