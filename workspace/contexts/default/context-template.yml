# Default Context Template
#
# This is the base context template that all panels use by default.
# It provides the paneldev skill for agent reference documentation.
#
# Panels can extend this template or specify their own custom template
# via the templateSpec option.

name: default
description: Base context template with paneldev skill for agent development

# Include paneldev skill for agent development documentation
structure:
  /workspace/skills/paneldev: skills/paneldev
