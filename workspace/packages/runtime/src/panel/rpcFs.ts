/**
 * RPC-backed RuntimeFs implementation — re-exported from shared.
 *
 * Kept at this path for backward compatibility with existing panel imports.
 */

export { createRpcFs } from "../shared/rpcFs.js";
