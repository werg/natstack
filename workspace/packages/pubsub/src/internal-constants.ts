/**
 * Internal protocol keys shared across PubSub transports and the channel DO.
 */

/** Metadata key used to distinguish participant process/session replacements. */
export const PARTICIPANT_SESSION_METADATA_KEY = "__participantSessionId";
